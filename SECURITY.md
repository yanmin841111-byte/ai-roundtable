**繁體中文** | [English](SECURITY.en.md)

# 安全性政策

## 支援的版本

目前只維護 `main` 分支的最新版本。

## 回報漏洞

請不要用公開 issue 回報安全性問題。

請到本專案 GitHub 頁面的 **Security → Report a vulnerability** 私下回報。如果看不到這個選項,請開一個 issue,標題寫「請求私下聯絡方式」,內容**不要**包含任何漏洞細節,維護者會再聯絡你。

回報時請盡量附上:

- 受影響的檔案或功能
- 重現步驟或概念驗證
- 可能造成的影響

維護者會盡快確認收到並評估。修正發布前,請先不要公開細節。

## 範圍

以下是預期中的行為,不算漏洞:

- 成員開啟「允許修改檔案與執行指令」後,AI CLI 在工作目錄內建立、修改、刪除檔案或執行指令。
- 使用者自行安裝的擴充以使用者的帳號權限執行指令;JS 外掛擁有完整的 Node.js 權限。
- 透過使用者自己設定的 CLI 或 API 送出對話內容與附件。

以下類型的問題歡迎回報:

- renderer 或對話內容能讓主程序讀寫預期以外的檔案(例如繞過歷史紀錄、附件、擴充的路徑檢查)
- 未經使用者操作就執行指令或載入擴充
- API key 以明文寫入磁碟、日誌,或傳給 renderer
- 附件驗證被繞過,導致讀取或覆寫工作目錄以外的檔案

## 使用建議

- 工作目錄請使用獨立資料夾並用 git 管理,不要指到家目錄或重要專案。
- 只安裝你看得懂、信任的擴充。
- API key 請在擴充編輯器填入(以作業系統安全儲存加密),或使用環境變數,不要寫進擴充檔。
