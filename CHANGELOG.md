**繁體中文** | [English](CHANGELOG.en.md)

# 變更紀錄

格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/),版本號遵循 [語意化版本](https://semver.org/lang/zh-TW/)。

## [0.2.1] - 2026-09-22

### 修正

- 將既有藍色雙圓標誌製作為多尺寸 macOS app 圖示,套用到 Finder、Dock 與安裝包,取代 Electron 預設圖示。側欄與開發模式使用同一份 PNG;可用 `npm run icons` 重新產生圖示資源。

## [0.2.0] - 2026-09-22

本版新增內建終端、測試先行、自動驗證、環境修復、停損、陣容與驗收工作區,並調整 UI / UX。

### 新增

- 結果卡改以待處理事項、驗證紀錄與檔案改動為主,成員放行不再等同可合併。保存檢查時間、檔案範圍、指令、結束碼與有限長度的輸出;未支援、超過上限、無法執行與因前一道失敗而略過的檢查分別列出。歷史紀錄與文字匯出也保留這些證據;舊紀錄沒有證據時明講。
- 人工驗收可手動開始、暫停,標記已驗收或未完成,並匯出本次 JSON 紀錄。計時存在本機 Electron localStorage,不上傳,與會議紀錄分開;重開 app 不累加離線時間。這是手動區段用時,不是自動量到的實際工作時間,也不代表已證明縮短驗收時間。原任務用時仍不含討論與分工。
- 驗證證據綁定有界的內容指紋。回到 app、手動比對、驗收及匯出時,會比對目前工作階段最新任務的檔案與驗證指令;這不是持續監控,也不是檔案系統隔離。指紋不含相依套件、快取、版本控制與 app 暫存目錄,最多涵蓋 10,000 個檔案、20 MiB;範圍不完整、無法讀取或含符號連結時顯示未知,不代表環境或需求正確性已驗證。載入的歷史紀錄不視為目前版本的證據,也不拿來授權執行指令。
- 最新任務可在確認目前目錄及設定指令後重新驗證,不呼叫模型、不自動修復或回退。驗證指令仍可能改檔,檢查期間內容改變時證據會標為過期。舊驗證紀錄保留可查,舊審查結論與人工驗收不自動套用到新版本。JSON 匯出含驗證證據,並區分歷史驗收與目前版本驗收;一般任務仍可記錄人工判定,但不宣稱檔案版本已驗證。

- 交叉審查改用乾淨的 context:審查者只看任務要求、對方的回報、實際改動與自動驗證結果,看不到討論與執行過程,也不續接自己的對話記憶。實際做過的檔案操作改由提示詞直接附上。
- 測試先行流程(討論 → 寫測試 → 實作 → 交叉審查):先把驗收條件寫成測試,實作回合鎖住那些測試。
- 專案規則:工作目錄的 `CLAUDE.md`、`AGENTS.md` 自動放進每位成員的系統提示。
- 停損:結果卡可以一鍵把工作目錄還原到任務開始前;還原不了的檔案照實列出。
- 驗證指令可以設多道(一行一道,依序執行),一道沒過就停,並說明是哪一道、前面哪幾道過了。
- 平行執行時多位成員改到同一個檔案會被指出來,並提醒審查者確認內容有沒有被覆蓋掉一半。
- 工作模式(輸入框旁可切換):「寫程式」會自動驗證改動、並鎖住任務開始前就存在的測試檔——修復回合不能靠改測試讓結果變綠,要讓測試通過請改實作;「一般任務」(文件、分析、腦力激盪)不做這些。陣容也會記住工作模式。
- 環境問題的統一修復流程:偵測到問題就照實說一句話,再給同一顆「在終端執行」(指令填進內建終端,不自動執行)或「打開安裝說明」。具體修掉的幾件事:這台 Mac 的 git 不能用(沒裝開發者工具、或沒同意 Xcode 授權)以前會被講成「這個工作目錄不是 Git 版本庫」,還叫人去 git init——照做也不會好;現在照實說原因、附上修復指令,且「檔案改動」自動退回任務前後的比對,功能不中斷。沒裝開發者工具的 Mac 上也不再每次呼叫 git,避免系統跳出安裝視窗。成員的模型設定新增狀態卡:選模型之前就說清楚這個 CLI / API 有沒有裝、有沒有登入、端點連不連得上。對話裡的失敗也算:一個回合失敗時,app 會順手確認一次那個 CLI / API 的健康狀態,把下一步接在錯誤訊息下面(沒登入 → 登入指令、沒裝 → 安裝說明、缺 key → 打開設定);健康檢查說一切正常就不給按鈕,不硬湊。修復建議不寫進歷史紀錄——環境會變,而紀錄檔可能被手動改過。驗證指令也分得開了:設定的指令根本不存在時說「找不到指令」並給「打開設定」,不再只說「失敗(結束代碼 127)」害人去翻程式碼。擴充範本可以宣告 `fixCommand`(例如 `ollama serve`)與 `docsUrl`,JS 外掛可以在 `check()` 與 `run()` 的回傳值帶 `fix`,介面照同一套呈現。
- 內建終端:⌘J 或工具列的「終端」打開右側面板,開在跟成員同一個工作目錄。分頁跑在真正的 pty 上(顏色、Ctrl-C、vim 與各家 AI CLI 都正常),可以開多個分頁、拖左緣調寬度、跟著主題換色,寬度會記住。pty 借 macOS 內建的 expect 取得,不引入任何原生模組,打包與安裝方式不變。
- 自動驗證:執行與修復之後,app 自己檢查改動的 `.js`/`.json` 載不載得起來,並執行你設定的「驗證指令」(例如 `npm test`)。結果交給審查者,沒過就一定要修,也寫進結果卡——「審查通過」不再只是模型說通過。評測量到,審查者讀完檔案照樣會放行載不起來的程式。語法檢查在 app 裡要以 node 身分執行(`ELECTRON_RUN_AS_NODE`):少了它,`process.execPath` 是 Electron,`--check` 會被忽略而變成「執行那個檔案」——壞掉的檔案被判通過,成員寫的測試檔反而被跑起來。
- 單人 vs 圓桌對照實驗(`npm run eval:ab`):同一個模型、同一道題,一個人做完和經過審查、修復各跑幾次,用模型看不到的隱藏測試計分,比較做對的比例與花的時間;審查者可以換成另一個模型。加 `--journal` 可以續跑:每跑完一次記一行,中斷後同一個指令只補缺的次數,不同程式版本的紀錄不沿用而且會說出來。
- 審查品質評測(`npm run eval`):真的模型當審查者跑六道固定題目——明顯的錯、正確的對照、看起來可疑但正確、錯在附上內容的上限之外、錯在改動以外的檔案、只分析不改檔——輸出分數。結果檔只有數字,可以放心分享。
- 陣容:把誰上場、各自的角色、主持人、流程與討論回合數存起來,之後在側欄一鍵換回來。陣容只記得是哪幾位成員,不動他們的 CLI、模型或金鑰;套用後又改過設定會標出「已修改」。
- API 成員(Ollama 與雲端 API 範本)可以用三個受限的檔案工具讀檔、改檔。範本明確開啟、成員允許改檔、而且有另一位成員能審查時才會提供;每一次呼叫都記進會議紀錄,審查者看得到實際做了什麼。
- 交叉審查時,每位審查者都看得到實際改動,方式依它的能力而定:CLI 成員自己開檔;開了檔案工具的 API 成員拿到唯讀的 `read_file`,同時附上檔案內容;其他成員直接附上內容。改動由工作目錄在執行前後的快照找出,不需要 git。
- 審查訊息顯示結論徽章(審查通過、提出問題、審查未完成)與「審查者看了什麼」;點檔名直接打開「檔案改動」並跳到那個檔案。
- 模型能力:API 成員所選的模型能不能呼叫工具、能不能看圖,顯示在成員卡片與成員設定。Ollama 自動查、端點附帶的模型資料直接採用;付費 API 要在成員設定按「測試」才會送出請求。已知不能呼叫工具的模型當成唯讀成員;已知不能看圖的模型不再收到圖片,送出前的提醒也會點名它。
- 工作目錄不是 git repo 時(預設工作區就不是),「檔案改動」也看得到成員改了什麼:任務開始前記下小型文字檔的內容(只放記憶體),列出最近一次任務開始以來的改動與逐行對照。
- 擴充編輯器可以設定「每回合逾時上限」(以分鐘為單位);擴充逾時時,錯誤訊息會說明在哪裡調高。
- 任務結果卡:分工任務結束時,一次看完每位成員的結果與審查結論、這次任務改了哪些檔案(點一下跳到檔案改動)、花了多少時間與 token。
- 失敗的 @ 指定回覆可以一鍵重試。
- 長回合顯示目前在做什麼、經過時間,以及太久沒有進度的提示。
- 偵測「已安裝但尚未登入」的 Claude Code 與 Codex,並說明要執行哪個指令登入。
- 附上的圖片送不到某位成員時,送出前先提醒。
- 開發:`npm run harness:ui` 驅動真正的 app 跑介面情境並留下截圖,CI 也會跑,失敗時上傳截圖。

### 變更

- 交叉審查補上兩個漏洞(由評測 `eval:ab` 找到):執行到一半失敗、但已經改了檔案的成員,以前直接跳過審查,寫壞的檔案沒有人看,現在照樣送審;修復之後,原本的審查者會再複查一次,複查通過才算「審查通過」,仍有問題就帶進總結。審查提示也改成逐條對照任務的要求,不影響正確性的建議不再算需要修正。
- 授權從 MIT 改為 [PolyForm Noncommercial 1.0.0](LICENSE):可以免費用於個人、研究、教育與非營利用途,不得用於商業用途。改用之前公開的版本(包括 v0.1.0)仍適用 MIT。
- 介面改用較安靜的石墨中性色與藍色主操作,減少光暈、裝飾漸層及大圓角;統一工具圖示、鍵盤焦點與中英文提示。結果卡先顯示證據與改動,再提供人工驗收,成員明細可展開;已驗收版本不可重複提交。輸入區使用精簡流程名稱並保留完整提示,終端開啟後自動換行,附件警告不再截斷。成員列支援鍵盤操作,深色模式提高主按鈕對比。
- 主持人的分工原文(多半是 JSON)成功解析後收起來,只留「分工結果」卡片;解析失敗的照樣完整顯示。
- 主程序產生的系統訊息、錯誤與 API 轉接器的訊息都跟著介面語言;給模型看的檔案工具說明、錯誤訊息與記憶裡的佔位文字也一樣,英文會議裡模型不再收到中英混雜的指令。
- 狀態燈號只在真的能用時亮綠燈:開設定時會驗證雲端 API key,連不上的端點顯示可照做的提示,成員卡片會標出沒安裝或沒登入的 CLI。
- 「檔案改動」的路徑一律相對於 repo 根目錄。

### 修正

- 唯讀的 Claude Code 成員每次都失敗:app 多傳了一個現行 CLI 不存在的 `--restricted`,claude 直接以「unknown option」結束。唯讀模式只需要 `--permission-mode dontAsk`(實測:被要求寫檔時會拒絕)。假的 CLI 什麼參數都收,所以舊測試全綠;現在會拿真的 `claude --help` 對照。
- 只分析、不改檔的工作,審查者指出回報的結論錯了,卻因為「不必改檔」照樣宣告沒問題,錯的結論就被放行。審查提示現在明確說明:回報的說明或結論有錯也算需要修正。本機 Qwen 在評測的這一題從 6 次判對 3 次提升到 6 次全對。
- 成員某一回合失敗後,下一回合會重新收到它沒看到的內容,不再失去前情。
- 標記為隱藏的元素不會再因為樣式而佔版面(例如每則訊息下面多出一個空白的警告框)。
- 審查的一連串修正:中文檔名、工作目錄在子資料夾或被 .gitignore 忽略、成員自己 commit、執行前就改過的檔案都能正確列出;模型不接受工具時改成不帶工具重送;附給審查者的檔案內容不留在 API 成員的對話記憶;快照期間按停止會真的停止。
- `read_file` 的 `limit` 超過 65536 時自動夾到上限,不再整次拒絕;只有內容未完整回傳時才標 `truncated`。SHA、唯一原文比對、禁止覆蓋新檔與每回合額度等保護維持不變。
- 寫程式流程結束前仍有內建語法檢查失敗時,自動回退並重驗:執行後語法原本通過的只收回修復回合,否則還原整次任務。修復回合報錯也會重驗。結果卡列出回退後的檔案與驗證結果,被撤回的成果不算交付;缺基準點、部分還原與還原失敗會明示。僅驗證指令失敗不自動回退。

## [0.1.0] - 2026-09-17

第一個版本。

### 新增

- 圓桌流程:成員輪流討論、主持人分工、平行執行、交叉審查、修復回合與總結;另有「只討論,不執行」模式。
- 內建 Claude Code、Codex CLI、Cursor CLI 與自訂指令轉接器,模型清單從各 CLI 的本機快取讀取。
- 擴充系統:用 JSON 描述 CLI 或 OpenAI 相容 API,或寫 JS 外掛;附 Grok、Kimi、DeepSeek、Gemini、OpenRouter、Ollama 與空白範本。
- 擴充編輯器的基本設定分頁、API 連線測試,以及用作業系統安全儲存加密保存 API key。
- 附件:拖放或選擇圖片、文字檔、PDF,依成員能力提供路徑、內嵌文字或圖片。
- 歷史對話:任務結束自動保存,可預覽、刪除、匯出成 Markdown,並能載入後繼續討論。
- `@名稱` 指定成員:只讓被指定的成員回覆;進行中指定時,任務結束前會確保對方回覆。
- 平行發言(執行、審查、修復、同時指定多位成員)每列並排三張顯示。
- 跨 CLI 與 API 的用量正規化與總計。
- 淺色、深色與跟隨系統主題,以及文字大小設定。
- 介面語言:繁體中文與 English(可跟隨系統),系統訊息、提示詞與匯出的 Markdown 一併切換。

### 變更

- 主程序、介面與測試改用 TypeScript(strict)撰寫;`npm start` 會先建置到 `dist/`,介面由 esbuild 打包。
- 主程序與介面共用 IPC 型別定義。
- 新增 GitHub Actions CI,每次 push 與 pull request 都跑型別檢查、測試、建置與端對端測試(`npm run e2e`,用假成員跑完整流程)。
- 內建 Claude Code / Codex CLI 轉接器有了重播 stream-json 事件的測試。
- `npm run dist` 產生 Apple Silicon 與 Intel 兩個 dmg 到 `release/`(ad-hoc 簽章、未公證);推送 `v*` 標籤會自動建置並附到 GitHub Release。

### 安全

- 擴充檔裡的明文 `apiKey` 欄位停用;載入時自動搬到安全儲存,確定存好才改寫檔案。
- `secrets.json` 損壞時先備份原檔,不會被新的 key 覆寫。
- 附件、歷史紀錄、擴充檔的路徑一律在主程序驗證。

[未發布]: https://github.com/yanmin841111-byte/ai-roundtable/compare/481bb88...HEAD
[0.2.0]: https://github.com/yanmin841111-byte/ai-roundtable/compare/v0.1.0...481bb88
[0.1.0]: https://github.com/yanmin841111-byte/ai-roundtable/releases/tag/v0.1.0
